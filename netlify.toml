[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

# Redirect all routes to Next.js (handled by Next.js routing)
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/next"
  status = 200
